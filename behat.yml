default:
    suites:
        default:
            contexts:
                - FeatureContext:
                    executable: bin/autogiro2xml
        phar:
            contexts:
                - FeatureContext:
                    executable: autogiro2xml.phar
